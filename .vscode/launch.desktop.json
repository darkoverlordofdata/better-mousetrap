{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "cwd": "${workspaceRoot}/bin",
            "program": "${workspaceRoot}/bin/test",
            "args": []
        }
    ]
}